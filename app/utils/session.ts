/** localStorage key for JWT from the Django API (`Authorization: Bearer`). */
export const TOKEN_KEY = 'hireonix-token';

/** localStorage key for cached authenticated user profile. */
export const USER_PROFILE_KEY = 'hireonix-user-profile';
